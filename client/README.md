# Client (Vite + React)

## Run
cd client
npm install
npm run dev

The frontend will run on http://localhost:3000 and talk to the backend at http://localhost:5000 by default. Change VITE_API_URL in client/.env if needed.
